<form method="post" action="{{ path('app_food_delete', {'id': food.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce repas ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ food.id) }}">
    <button>
        {{ include('_partials/icons/_trash.html.twig') }}
    </button>
</form>
