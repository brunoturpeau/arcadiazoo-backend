{{ form_start(form) }}
    {{ form_row(form.created_at) }}
    {{ form_row(form.time) }}
    {{ form_row(form.animal) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
