<form method="post" action="{{ path('app_eating_delete', {'id': eating.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eating.id) }}">
    <button class="btn">Supprimer</button>
</form>
