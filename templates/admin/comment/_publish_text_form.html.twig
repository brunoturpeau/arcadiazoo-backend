<form method="post" action="{{ path('app_comment_publish', {'id': comment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('publish' ~ comment.id) }}">
    <button>
        {{ include('_partials/icons/_check.html.twig') }} Publier
    </button>
</form>
